# /new-task

1. /run-tests — must be green before starting
2. git add -A && git commit -m "snapshot before next task"
3. Read next task from TASK_PROMPTS.md
4. Enter Plan Mode (Shift+Tab), research codebase, propose plan
5. Wait for approval before writing implementation code
